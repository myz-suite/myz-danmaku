# Changelog

# v0.1.2 自定义弹幕体验与多段注释修复

- **时间轴提示**：在 YouTube 进度条上绘制弹幕指示条，包含时间码的评论会在时间线上显示高亮刻度，方便预判弹幕密度。
- **多时间码评论拆分**：支持将按行列出多个时间点的“指南式”评论拆解为多条弹幕，并在读取旧缓存时自动修正重复展示整段内容的问题。
- **字体大小与抓取页数设置**：Popup 设置面板新增字体缩放与评论抓取页数选项（1–60 页），并将偏好保存在 `chrome.storage.sync` 中，内容脚本会即时应用。
- **弹幕视觉调优**：浮层字体遵循用户自定义缩放，弹幕动画去掉缓慢淡入/淡出，避免视觉拖影，键盘事件监听也改为 passive 以减少性能警告。
- **容错与同步改进**：延迟时间轴容器渲染以适配 YouTube 的动态 DOM，设置变更会触发内容脚本重新抓取并刷新徽标 / 缓存。
